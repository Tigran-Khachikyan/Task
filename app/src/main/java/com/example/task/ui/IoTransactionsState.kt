package com.example.task.ui

enum class IoTransactionsState {
    LOADING_SUCCEED,
    LOADING_ERROR,
    REMOVING_SUCCEED,
    REMOVING_FAILED,
    DOWNLOADING_SUCCEED,
    DOWNLOADING_FAILED,
    NO_NETWORK,
    NONE
}